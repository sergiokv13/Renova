<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-62556890-1', 'auto');
  ga('send', 'pageview');
</script>

<script>
  $(function () { // wait for document ready

      // init
      var controller = new ScrollMagic.Controller();

      // add scene for pin
      var scene = new ScrollMagic.Scene({
              triggerElement: "#pinContainer",
              triggerHook: "onLeave",
              duration: "400%"
            })
            .setPin("#pinContainer")
            .addTo(controller); // add indicators (requires plugin)

            // add scenes for animations
      new ScrollMagic.Scene({
          triggerElement: "#triggers #t1",
          triggerHook: "onLeave",
          duration: "100%"
      })
      .setTween(new TimelineMax()
        .to("section.panelturqoise .additionalContentturqoise", 1, {y: "-100%", ease: Linear.easeNone}) // scroll Content
        .fromTo("section.panelgreen",    1, {x:  "100%"}, {x: "0%", ease: Linear.easeNone})  // in from right
      )
      .addTo(controller);
          
      new ScrollMagic.Scene({
          triggerElement: "#triggers #t2",
          triggerHook: "onLeave",
          duration: "100%"
      })
      .setTween(new TimelineMax()
          .to("section.panelgreen .additionalContentgreen", 1, {y: "-100%", ease: Linear.easeNone}) // scroll Content
          .fromTo("section.panelbordeaux",    1, {x:  "-100%"}, {x: "0%", ease: Linear.easeNone})  // in from right
      )
      .addTo(controller);
          
      new ScrollMagic.Scene({
          triggerElement: "#triggers #t3",
          triggerHook: "onLeave",
          duration: "100%"
      })
      .setTween(new TimelineMax()
          .to("section.panelbordeaux .additionalContentbordeaux", 1, {y: "-100%", ease: Linear.easeNone}) // scroll Content
          .fromTo("section.panelred",    1, {x:  "100%"}, {x: "0%", ease: Linear.easeNone})  // in from right
      )
      .addTo(controller); 

      new ScrollMagic.Scene({
          triggerElement: "#triggers #t4",
          triggerHook: "onLeave",
          duration: "100%"
      })
      .setTween(new TimelineMax()
          .to("section.panelred .additionalContentred", 1, {y: "-100%", ease: Linear.easeNone}) // scroll Content
          .fromTo("section.panelwhite",    1, {x:  "-100%"}, {x: "0%", ease: Linear.easeNone})   // in from right
      )
      .addTo(controller);

        new ScrollMagic.Scene({
          triggerElement: "#triggers #t5",
          triggerHook: "onLeave",
          duration: "100%"
      })
      .setTween(new TimelineMax()
          .to("section.panelwhite .additionalContentwhite", 1, {y: "-150%", ease: Linear.easeNone}) // scroll Content
      )
      .addTo(controller);  


  });
</script>

<script type="text/javascript">
  $('#loading').show();
  $('#head').hide();
  $('#contento').hide();

  // then when the #content div has loaded
  $(window).bind('load', function() {
    $('#loading').hide();
    $('#head').fadeIn('slow');
    $('#contento').fadeIn('slow');
  });
</script>

<script type="text/javascript">
  $(function() {
   $('.notice').delay(500).fadeIn('normal', function() {
      $(this).delay(2500).fadeOut();
     });
  });
</script>